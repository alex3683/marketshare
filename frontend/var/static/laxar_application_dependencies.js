define( [
   'laxar-input-control/ax-input-control',
   'laxar-application/includes/widgets/marketshare/receipt-input-widget/receipt-input-widget'
], function() {
   'use strict';

   var modules = [].slice.call( arguments );
   return {
      'angular': modules.slice( 0, 2 )
   };
} );
